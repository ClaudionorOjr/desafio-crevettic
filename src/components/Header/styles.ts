import styled from '@emotion/styled'

export const CustomHeader = styled.header`
  display: flex;
  align-items: center;

  width: 100%;
  height: 5rem;
  background: var(--orange);

  img {
    height: 4rem;
    margin-left: 1.5rem;
  }
`
